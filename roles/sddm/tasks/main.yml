---

- name: Install SDDM
  community.general.pacman:
    name:
      - sddm
  become: true
  tags: [sddm]

- name: Enable the SDDM service
  ansible.builtin.service:
    name: sddm
    enabled: true
  become: true
  tags: [sddm]
