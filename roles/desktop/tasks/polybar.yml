---

- name: Install polybar
  become: true
  aur: name=polybar user=aurbuilder

- name: Prepare polybar configuration directories
  file: path=~/.config/polybar state=directory

- name: Install polybar configuration
  copy: src=polybar/config dest=~/.config/polybar/config

- name: Install polybar startup script
  template: src=polybar/start.sh dest=~/.config/polybar/start.sh mode=0755
