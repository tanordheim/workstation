---

- name: Install base packages
  become: true
  pacman: name={{ item }} state=present
  with_items:
    - bc
    - xclip

- name: Install base AUR packages
  aur: name={{ item }} user=aurbuilder
  with_items:
    - yaourt
