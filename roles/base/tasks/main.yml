---

- name: Update PacMan cache
  pacman: update_cache=yes

- name: Install zsh
  pacman: name={{ item }} state=present
  with_items:
    - zsh
    - zsh-completions

- name: Change user shell
  user:
    name: "{{ ansible_user }}"
    shell: /bin/zsh
