---

- name: Install zsh
  pacman: name={{ item }} state=present
  with_items:
    - zsh
    - zsh-completions

- name: Change shell to zsh
  user:
    name: "{{ ansible_user }}"
    shell: /bin/zsh

